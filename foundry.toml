[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "shanghai"

# Molt EVM — onlySpawnerToken modifier bypass on Base
# This is a simulation PoC, not a fork replay
# To fork Base mainnet, uncomment and set:
# [profile.fork]
# eth_rpc_url = "https://base-mainnet.g.alchemy.com/v2/YOUR_KEY"
# fork_block_number = <block_before_exploit>

[fuzz]
runs = 256
